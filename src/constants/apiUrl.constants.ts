/**
 * Constants for API URL's.
 */
export const API_URLS = {
  USER: {
    SIGNUP: '/user/',
    SIGNIN: '/user/login/',
    REFRESH: '/user/refresh/',
    LOGOUT: '/user/logout/',
    PROFILE: '/user/',
  },
} as const;
