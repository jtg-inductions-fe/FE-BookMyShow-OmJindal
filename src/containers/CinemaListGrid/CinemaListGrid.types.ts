import type { CinemaListFilterType } from '@/containers/CinemaListFilter';

/**
 *  Props for CinemaList Grid component
 */
export type CinemaListGridProps = {
  /**
   * The filters for the cinemaList query.
   */
  filters: CinemaListFilterType;
};
